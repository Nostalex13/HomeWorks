define(
   'script',
   ['jquery', 'model', 'view'],
   function(model, view) {

      $(function() {
         // let model = new Model();
         // let view = new View(model);
         // let controller = new Controller(model, view);
      });

      return {

      };
   }
);
